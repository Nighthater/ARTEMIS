function [OUTPUT] = Differential_Solver(gravity)
    
    global g  
    tspan = [0 tspan_end]
    x0 =0; u0 = 10; z0 = 0; v0 = 10
end


function dy = dgl_only_gravity(t,y)
    global g
    dy(1,1) = y(2);
    dy(1,2) = 0;
    dy(2,1) = y(4);
    dy(1,2) = -g;
end